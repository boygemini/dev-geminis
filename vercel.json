{
	"version": 2,
	"builds": [
		{
			"src": "src/server.js",
			"use": "@vercel/node"
		},
		{
			"src": "client/assets",
			"use": "@vercel/static-build",
			"config": { "outputDirectory": "dist/client/assets" }
		}
	],
	"routes": [
		{ "src": "/api/.*", "dest": "src/server.js" },
		{
			"src": "/(.*)",
			"dest": "/client/assets/$1"
		}
	]
}
